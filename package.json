{
  "name": "vector-saint",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A controller-first neon vector twin-stick arcade shooter prototype built with TypeScript, Vite, Three.js, and WebGL.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "three": "^0.165.0"
  },
  "devDependencies": {
    "@types/three": "^0.165.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
